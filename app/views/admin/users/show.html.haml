.row.justify-content-md-center.text-center
  .col-sm-4
    %h2
      #{t('user_id')}: #{@model.id}
    %ul.list-group
      %li.list-group-item
        %b
          #{t('activerecord.attributes.user.first_name')}:
        = @model.first_name
      %li.list-group-item
        %b
          #{t('activerecord.attributes.user.last_name')}:
        = @model.last_name
      %li.list-group-item
        %b
          #{t('activerecord.attributes.user.phone')}:
        = @model.phone
      %li.list-group-item
        %b Email:
        = @model.email
      %li.list-group-item
        %b
          #{t('activerecord.attributes.user.role')}:
        = t("role.#{@model.role}")
      -unless @model.affiliate.nil?
        %li.list-group-item
          %b
            #{t('affiliate')}:
          = @model.affiliate.name

    - if can? :edit, User
      = link_to t('edit'), edit_admin_user_path(@model.id), class: 'btn btn-outline-info btn-sm'
    - if can? :destroy, User
      = link_to t('delete'), admin_user_path(@model), method: :delete, data: { confirm: t('confirmation') }, class: 'btn btn-outline-danger btn-sm'
    %div/
    = link_to t('to_users_list'), admin_users_path




            


